<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.qm</groupId>
    <artifactId>QQMinecarts</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>QQMinecarts</name>
    <description>Быстрые вагонетки с плавным переключением скоростей (F/ЛКМ) для Paper/Leaf 1.21.11–26.3+</description>
    <url>https://github.com/AllF1RE</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>

        <!-- API ядра Leaf 26.2 (форк Paper с регионами). https://github.com/Winds-Studio/Leaf -->
        <!-- Если появятся версии 26.3+ — просто обновите эту версию. -->
        <leaf.version>26.2.local-SNAPSHOT</leaf.version>
    </properties>

    <repositories>
        <repository>
            <id>leafmc</id>
            <url>https://maven.leafmc.one/snapshots/</url>
        </repository>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Leaf / Paper API (для компиляции; на сервере уже есть) -->
        <dependency>
            <groupId>cn.dreeam.leaf</groupId>
            <artifactId>leaf-api</artifactId>
            <version>${leaf.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>QQMinecarts-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>25</release>
                    <encoding>UTF-8</encoding>
                    <!-- Обход бага javac-in-process на JDK 25 (CMOPL-404) -->
                    <fork>true</fork>
                    <forceLegacyJavacApi>true</forceLegacyJavacApi>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>